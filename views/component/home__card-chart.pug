mixin home__card-chart({id='',title='card title', subtitle='',icon='', info='Information will appear here when available',links=[], src=''})
    div(class='home__card-chart')
        .card
            .card__header
                //- img(src= icon).card__icon
                h1.card__title=title
                h2.card__sub-title=subtitle
            .card__body
                div(id=id+'__chart' class="card__line-chart")
            .card__info
                .card__info-border 
                .card-info-text.p=info
                .card__info__buttons
                    each l in links
                        a(href=l.href class="card__link").button #{l.name}
    //- TODO: pass in args to remove hard-coded values in script, generalise script (issue #27)
    script(src=src type="text/javascript" )
    script
        | async function exec(){
        |   var plot = await createChart();
        |   plot()
        | }
        | exec()
    

        
            
        
      

        
                        
                    
